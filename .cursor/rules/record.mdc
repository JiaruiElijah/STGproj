---
alwaysApply: true
---
# 开发记录规范（record）

## 目的与用途
• **开发记录**用于记录本项目每次功能新增与修改，形成可追溯的日志，便于日后查阅、接手和排查问题。
• Agent 在完成开发后必须按本规范追加记录，不得遗漏。

## 记录存放位置
• 统一写入项目根目录下的 **`RECORD.md`**。
• 若项目内已有约定的开发日志路径（如 `docs/开发记录.md`），以项目内约定为准；否则使用 `RECORD.md`。

## 记录格式（推荐模板）
每条记录建议包含以下字段，按时间倒序（新记录写在文件顶部）：

```markdown
## [YYYY-MM-DD] 功能/修改标题
- **类型**：新增功能 | 修改功能 | Bug 修复 | 重构
- **涉及文件**：`path/to/file1.js`、`path/to/file2.js`
- **说明**：简要描述做了什么、关键逻辑或注意事项。
- （可选）**对应 Plan**：本次实现对应的规划步骤或对话摘要。
```

## 何时参考开发记录
• **开发前**：接手新需求或延续功能时，先阅读 RECORD.md 了解近期改动和涉及模块，避免重复劳动或冲突。
• **排查问题**：出现异常或回归时，根据时间线和“涉及文件”快速定位可能相关的修改。
• **规划与评审**：做新 Plan 或代码评审时，可结合记录确认依赖关系和已有实现。

## Agent 行为要求
• 完成任意功能开发或实质性修改后，**在同一轮对话内**主动向 RECORD.md 追加一条记录，使用上述格式。
• 若 RECORD.md 不存在，应先创建该文件并写入第一条记录；若已存在，则在现有内容**顶部**插入新记录以保持倒序。
• 记录内容需具体、可检索，避免“完成某模块”“优化代码”等无信息量表述。
